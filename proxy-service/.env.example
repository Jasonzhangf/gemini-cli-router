# Gemini CLI Router Configuration Example
# Copy this file to .env and configure your settings

# Authentication Configuration
# GCR_API_KEY=your_gemini_api_key_here  # Optional: Gemini API key (if not set, uses OAuth)
GCR_TARGET_API_KEY=your_target_provider_api_key_here  # Required: Target provider API key

# SHUAIHONG Provider Configuration (Default)
GCR_PROVIDER=shuaihong
GCR_BASE_URL=https://ai.shuaihong.fun/v1
GCR_MODEL=gemini-2.5-pro

# Alternative providers:
# GCR_PROVIDER=deepseek
# GCR_TARGET_API_KEY=your_deepseek_key
# GCR_MODEL=deepseek-chat

# GCR_PROVIDER=openai
# GCR_TARGET_API_KEY=your_openai_key
# GCR_MODEL=gpt-4

# GCR_PROVIDER=claude
# GCR_TARGET_API_KEY=your_anthropic_key
# GCR_MODEL=claude-3-sonnet-20240229

# Server Configuration
GCR_PORT=3458
GCR_HOST=localhost

# Debug Mode
GCR_DEBUG=false

# Custom Provider (if using GCR_PROVIDER=custom)
# GCR_CUSTOM_BASE_URL=https://your-custom-api.com/v1
# GCR_CUSTOM_ENDPOINT=/chat/completions
# GCR_CUSTOM_MODEL=your-custom-model